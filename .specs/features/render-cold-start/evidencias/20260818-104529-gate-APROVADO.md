# Evidência — Cold start do Render (APROVADO)

Data: 2026-08-18

## RC-01 — /health responde sem tocar no Firestore
API subida com `FIRESTORE_EMULATOR_HOST=127.0.0.1:9999` (nada escutando):
```
GET /health        -> {"status":"ok"}   HTTP 200   real 0m0.009s
GET /api/projects  -> HTTP 500
```
O contraste prova a independência: o alvo do keep-alive responde mesmo com o banco fora,
e em 9 ms — logo o ping não consome cota de leitura do Firestore.

## RC-02 / RC-03 — retry no getProjects

**RED (antes da implementação):**
```
Test Files  1 failed (1)
     Tests  3 failed | 2 passed (5)
```
As 3 falhas eram exatamente os casos de retry; os 2 que passavam não dependiam dele.

**GREEN (após a implementação, sem alterar os testes):**
```
Test Files  1 passed (1)
     Tests  5 passed (5)
```

Cobertura temporal: 3 tentativas de 15s + backoff de 3s e 6s = ~54s, acima do
cold start de ~1 min declarado pelo Render (margem coberta pelo ISR).

## RC-04 — orçamento de instance-hours
```
horas/dia com ping: 16   (cron */10 0-2,11-23 UTC = 08:00–00:00 BRT)
consumo estimado:   496 h/mês
cota Render:        750 h/mês
margem:             254 h
```
24/7 daria 744 h/mês — margem de 6 h, com risco de suspensão total dos serviços free.

## Gate Full
```
dotnet build ............ 0 warnings, 0 errors
dotnet test (API) ....... Passed! 12/12, 0 skipped
vitest (frontend) ....... 5/5
npm run lint ............ limpo
npm run build ........... Compiled successfully + TypeScript OK
```

## Credencial real (fora do emulador)
Contagem **somente leitura** contra o Firestore de produção `portfoliodb-9215c`:
```
credencial carregada; conectando ao Firestore REAL...
OK — colecao 'projects' tem 0 documento(s)
```
Valida o caminho `CredentialFactory` + `GoogleCredential` contra o serviço real, não só o
emulador. Nenhuma escrita foi feita.
