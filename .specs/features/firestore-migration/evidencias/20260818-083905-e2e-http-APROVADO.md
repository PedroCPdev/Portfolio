# Evidência — E2E HTTP contra a API real (APROVADO)

Data: 2026-08-18
Como: `dotnet run` real em 127.0.0.1:5199, `FIRESTORE_EMULATOR_HOST=127.0.0.1:8080`,
coleção isolada `projects_e2e`. Requisições por `curl`.

## FS-01 — build do FirestoreDb + seed no startup
```
info: PortfolioApi[0]
      [Seeder] 2 projetos semeados no Firestore.
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://127.0.0.1:5199
```

## FS-02 — GET /api/projects → 200, ordenado desc
```
HTTP 200 — 2 projetos
[0] id="MeogLkwcft6DrolnQ6Ci" title="Portfolio Frontend" createdAt=2026-08-18T11:37:22.893576Z
[1] id="pKVApvDyTsarnkVimBIO" title="Portfolio API"
```
JSON em camelCase, `tags` como array, nulos preservados (`imageUrl: null`) —
shape idêntico ao que `frontend/src/lib/api.ts` já consumia.

## FS-03 — GET /api/projects/{id} → 200 com id string
```
GET /api/projects/MeogLkwcft6DrolnQ6Ci
>>> HTTP 200   {"id":"MeogLkwcft6DrolnQ6Ci","title":"Portfolio Frontend",...}
```

## FS-04 — id inexistente → 404
```
GET /api/projects/id-inexistente-xyz   >>> HTTP 404
GET /api/projects/123                  >>> HTTP 404
```

## FS-06 — seed idempotente entre reinícios
Segundo boot da API contra a mesma coleção:
```
(nenhuma linha de seed no 2o boot — esperado)
2 projetos (esperado: 2)
```

## FS-07 — Firestore indisponível NÃO derruba a API  ← causa raiz do incidente
API subida com `FIRESTORE_EMULATOR_HOST=127.0.0.1:9999` (nada escutando ali):
```
Now listening on: http://127.0.0.1:5198
Application started. Press Ctrl+C to shut down.
Seed do Firestore falhou; a API segue no ar e /api/projects pode responder vazio. Ve...

POST /api/contact  >>> HTTP 200  {"success":true,"message":"Message sent!"}
GET  /api/projects >>> HTTP 500  (Grpc RpcException Unavailable)
```
Comportamento antigo: `throw` na FASE 2 → **a aplicação inteira não subia**.
Comportamento novo: degradação parcial — `/api/contact` intacto, `/api/projects` em 500
que o frontend já converte em estado vazio. O stack trace acima só aparece porque o teste
rodou em `Development`; em produção `UseExceptionHandler` + ProblemDetails devolvem resposta limpa.

## Não-regressão
`POST /api/contact` respondeu 200 no cenário normal também (EmailService não configurado
neste ambiente degrada silenciosamente — comportamento pré-existente, não alterado).
